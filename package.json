{
  "name": "project-btpn_betest",
  "version": "0.0.0",
  "description": "Technical test from BTPN",
  "files": [
    "build/src"
  ],
  "keywords": [],
  "scripts": {
    "test": "npx jest",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.4.5",
    "gts": "^5.0.0",
    "typescript": "~5.1.6"
  }
}
